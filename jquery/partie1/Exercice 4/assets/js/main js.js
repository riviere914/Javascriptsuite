$(document).ready(function() { // quand le document est chargé, je demande l'action suivante
$('#firstText').empty(); // disparition du premier text
$('#secondText').remove(); // supprimer les éléments

});

// important sur la tructure du DOM
